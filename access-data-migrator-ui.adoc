---
sidebar: sidebar
permalink: access-data-migrator-ui.html
keywords: google cloud, installation, control plane, worker vm, iam roles
summary: "Create VM instances on Google Cloud by enabling required APIs, preparing storage, configuring IAM roles, and deploying control plane and worker VMs."
---
= Access the NetApp Data Migrator UI 
:icons: font
:imagesdir: ./media/

[.lead]
After you deploy the control plane VM and verify that the services and pods are running successfully, access the NetApp Data Migrator UI and connect to the control plane, Linux workers, and Windows workers.

.Before you begin
You have the control plane VM private IP address, which you obtained when you link:deploy-control-plane-and-linux-workers.html[deployed the control plane VM]. You use the control plane VM private IP address to access the UI. 

.Steps
. Navigate to the control plane using a web browser: 
+
https://<control-plane-private-ip-address>/ 

. Use the default username and password to log in.
. Based on your migration type, select one of the following tabs:  
+
*NFS*: This option is for NFS migrations (Linux workers).
+
*SMB*: This option is for SMB migrations (Windows workers).

. Use the control plane to connect to the deployed worker VMs by following the onscreen instructions. 
+
On the home page, select *View Instruction to Setup Worker*. 
. Verify that the worker is successfully connected by checking the status in the *Workers* section. 
+
After the workers are successfully connected, you can configure the NetApp Data Migrator environment and run migration jobs. 

.What's next?
Learn about link:configure-netapp-data-migrator.html[configuring NetApp Data Migrator]